export function OfflineBanner() {
  return (
    <div className="banner-warning">
      You're offline — connect to the internet to use this feature. Manual entry is always available.
    </div>
  );
}
