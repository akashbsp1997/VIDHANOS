import { EmptyState } from '@/components/EmptyState';
import { Screen } from '@/components/Screen';

export function CitationDetailScreen() {
  return (
    <Screen>
      <EmptyState title="Citation detail coming up" />
    </Screen>
  );
}
